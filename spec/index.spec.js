const diff = require('../index');

describe('Object diff testing', () => {
  it('should exist', () => {
    expect(diff).toBeTruthy();
  });


  // if params are not valid
  // what params are not valid ?

  // if params are valid
  //

  it('should throw an error if input params are not valid or not exists', () => {

  });

  it('should see difference only in base -> target direction', () => {

  });

  it('should see difference between two 1 depth objects', () => {

  });

  it('should see difference between two 1 depth objects', () => {

  });

  it('should not see difference between two 1 depth objects', () => {

  });

  it('should not see difference between 1 depth objects', () => {

  });
});
